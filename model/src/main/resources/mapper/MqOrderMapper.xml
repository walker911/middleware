<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.debug.middleware.model.mapper.MqOrderMapper">

    <!-- 保存 -->
    <insert id="save" parameterType="com.debug.middleware.model.entity.MqOrder">
        insert into mq_order(order_id, business_time, memo) values (#{orderId}, #{businessTime}, #{memo})
    </insert>
</mapper>