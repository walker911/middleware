<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.debug.middleware.model.mapper.RedRobRecordMapper">

    <!-- 保存 -->
    <insert id="save" parameterType="com.debug.middleware.model.entity.RedRobRecord">
        insert into red_rob_record(user_id, red_packet, amount, rob_time, create_time) values
        (#{userId}, #{redPacket}, #{amount}, #{robTime}, #{createTime})
    </insert>

</mapper>